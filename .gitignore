# Dependencies
node_modules/
admin/node_modules/

# Build output
dist/
admin/dist/
drizzle/
*.tsbuildinfo

# Compiled binaries
vaultbase
vaultbase.exe
releases/*
!releases/.gitignore

# Bun build artifacts
.*.bun-build
index.js.map

# Data dirs (local DB, uploads, logs, secrets)
vaultbase_data/
vaultbase_data_test/
*.db
*.db-shm
*.db-wal
.secret

# Env / secrets
.env
.env.local
.env.*.local

# Worktrees / IDE
.worktrees/
.claude/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Internal scratch: roadmap, BUILD_TODO, threat models, future-features, perf
# baselines, superpowers plans/specs. All reside under vaultbase/docs and are
# NOT for public consumption (the public docs site is a sibling repo).
docs/

# Internal security-audit working files (the published advisory lives in
# SECURITY.md; these are pre-publication notes only).
SECURITY-AUDIT-*.md

# Local screenshots / scratch
images-claude/

# Shannon (Keygraph AI pentester) — local-only config + scan deliverables.
# Config holds plaintext throwaway-dev credentials; never commit.
.shannon/
shannon.config.yaml
shannon.yaml
shannon.yml
